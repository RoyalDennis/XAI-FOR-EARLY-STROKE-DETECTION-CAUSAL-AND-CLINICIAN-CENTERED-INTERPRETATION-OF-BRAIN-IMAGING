# XAI-FOR-EARLY-STROKE-DETECTION-CAUSAL-AND-CLINICIAN-CENTERED-INTERPRETATION-OF-BRAIN-IMAGING
This repository describes a framework that checks whether stroke-detection models follow sound clinical reasoning. It shows that a model with a 92% Dice score can still be unsafe because it depends on misleading image features missed by standard explanation tools.
